export function NotFoundPage() {
    return (
        <section className="card">
            <h1>Page Not Found</h1>
            <p className="lead">
                That page doesn’t exist. Use the navigation above to get back on track.
            </p>
        </section>
    )
}
